# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=system_health
DB_USER=postgres
DB_PASSWORD=your_password_here

# Application Configuration
PORT=3020
NODE_ENV=production

# Base path for serving the app from a subdirectory
# Leave empty for root serving, or set to subdirectory path
# Examples:
# BASE_PATH=
# BASE_PATH=/dashboard
# BASE_PATH=/health-monitor
BASE_PATH=